[rerere]
	enabled = true
[alias]
	graph = log --graph --oneline
[core]
	excludesfile = ~/.gitignore
  pager = less -LRc --mouse --wheel-lines=3
[pager]
  diff    = LESS='-LRc --mouse --wheel-lines=3' delta
  show    = LESS='-LRc --mouse --wheel-lines=3' delta
  log     = LESS='-LRc --mouse --wheel-lines=3' delta
  reflog  = LESS='-LRc --mouse --wheel-lines=3' delta
  status  = LESS='-LRc --mouse --wheel-lines=3' delta
[tig]
  mouse = true
[interactive]
	diffFilter = delta --color-only
[delta]
  navigate = true
  side-by-side = true
  line-numbers = true
[difftool]
	prompt = false
[merge]
	tool = vimdiff
  ff = no
[pull]
	rebase = true
[push]
  ff = no
[color "diff-highlight"]
	oldNormal = red bold
	oldHighlight = red bold 52
	newNormal = green bold
	newHighlight = green bold 82
[color "diff"]
	meta = 11
	frag = magenta bold
	commit = yellow bold
	old = red bold
	new = green bold
	whitespace = red reverse
[commit]
	gpgsign = true
